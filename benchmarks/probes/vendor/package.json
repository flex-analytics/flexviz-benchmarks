{
  "name": "ttfr-vendor",
  "private": true,
  "type": "module",
  "dependencies": {
    "@uwdata/vgplot": "0.30.0",
    "@duckdb/duckdb-wasm": "1.32.0",
    "@perspective-dev/client": "5.2.0",
    "@perspective-dev/server": "5.2.0",
    "@perspective-dev/viewer": "5.2.0",
    "@perspective-dev/viewer-charts": "5.2.0"
  },
  "//": "All four @perspective-dev packages are direct deps AND overridden: their inter-deps are declared as empty ranges (npm reads '' as '*'), so pinning only the viewer would silently drift the engine on any future install.",
  "overrides": {
    "@perspective-dev/client": "5.2.0",
    "@perspective-dev/server": "5.2.0",
    "@perspective-dev/viewer": "5.2.0",
    "@perspective-dev/viewer-charts": "5.2.0"
  },
  "devDependencies": { "esbuild": "0.24.0" }
}
